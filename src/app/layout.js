export const metadata = {
  title: 'Country Finder - Search Engine',
}

export default function RootLayout({ children }) {
  return (
    <html lang='en'>
      <body>{children}</body>
    </html>
  )
}
